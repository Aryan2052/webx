Got it! Here's the properly formatted **installation and usage guide** in Markdown that you can paste into your GitHub repo's `README.md` or a separate `INSTALL.md` file:

---

## 🚀 Gradio Web App (Browser Use Enabled)

### 🧪 1. Install Requirements

First, install all the required libraries and dependencies:

```bash
pip install -r requirements.txt
```

### 🧱 2. Install Playwright

After installing the requirements, install the browser dependencies for Playwright:

```bash
playwright install
```

### 🖥️ 3. Run the App

Start the application with:

```bash
python app.py
```

---

Let me know if you'd like to include virtual environment setup instructions or a `requirements.txt` sample too!
